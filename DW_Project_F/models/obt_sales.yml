version: 2

models:
  - name: obt_sales
    description: The sales fact table combined with all of its outrigger dimensions into one big table. Periodic Snapshot table with one row per item ordered. Includes product data.
